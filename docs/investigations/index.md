# Investigations

Investigation notes under `docs/` will live here. Existing pre-plan investigation material remains in the top-level `investigations/` directory until a later cleanup decides whether to move it.
